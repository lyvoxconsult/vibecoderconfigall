# Permissoes exemplo para OpenCode no Mac.
# Substitua os placeholders pelos caminhos reais descobertos localmente.

# Skills locais do ecossistema
# permission: external_directory
# pattern: "<HOME>/.agents/skills/*"
# action: allow

# Skills Claude, se existirem
# permission: external_directory
# pattern: "<HOME>/.claude/skills/*"
# action: allow

# Skills curadas deste repositorio
# permission: external_directory
# pattern: "<REPO_ROOT>/skills obrigatorias/*"
# action: allow

# Vault Obsidian confirmado pelo usuario
# permission: external_directory
# pattern: "<OBSIDIAN_VAULT>/*"
# action: allow
